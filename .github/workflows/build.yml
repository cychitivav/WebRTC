name: WebRTC Build

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "webRTC branch head. Ex: branch-heads/6167"
        required: true
        default: "branch-heads/0000"
        type: string
      ios:
        description: "Build iOS libraries"
        required: true
        default: true
        type: boolean
      macos:
        description: "Build macOS libraries"
        required: true
        default: true
        type: boolean
      maccatalyst:
        description: "Build macOS Catalyst libraries"
        required: true
        default: true
        type: boolean
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install OS dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential ninja-build python3 git curl wget lsb-release pkg-config

      - name: Configure CMake
        working-directory: ros_packages/webrtc
        run: |
          mkdir -p build_cmake
          cd build_cmake
          cmake ..

      - name: Build WebRTC
        working-directory: ros_packages/webrtc/build_cmake
        run: make -j$(nproc)

      - name: Create DEB Package
        working-directory: ros_packages/webrtc/build_cmake
        run: cpack -G DEB

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: webrtc-deb-package
          path: build_cmake/*.deb
